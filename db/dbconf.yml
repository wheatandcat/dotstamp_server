
test:
    driver: mymysql
    open: stamp_test/stamp/stamp

development:
    driver: mymysql
    open: stamp/stamp/stamp

customimport:
    driver: customdriver
    open: customdriver open
    import: github.com/custom/driver
    dialect: mysql

environment_variable_config:
    driver: $DB_DRIVER
    open: $DATABASE_URL
