
test:
    driver: mymysql
    open: stamp_test/root/

development:
    driver: mymysql
    open: stamp/root

customimport:
    driver: customdriver
    open: customdriver open
    import: github.com/custom/driver
    dialect: mysql

environment_variable_config:
    driver: $DB_DRIVER
    open: $DATABASE_URL
